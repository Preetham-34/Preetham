# Minimal coturn config for username/password auth (long-term credentials)
listening-port=3478
# tls-listening-port=5349   # uncomment when you add certs for TURN over TLS
# cert=/certs/fullchain.pem # for TLS
# pkey=/certs/privkey.pem   # for TLS

fingerprint
lt-cred-mech

# Replace with strong user/pass, also add more via userdb if needed
user=turnuser:turnpass

realm=yourdomain.com
total-quota=0
bps-capacity=0
no-stdout-log
simple-log
verbose
# For debugging only; remove in production
# log-file=/var/log/turn.log
# set-verbose
